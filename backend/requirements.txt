# === Backend requirements (FastAPI + RAG) ===
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
aiofiles>=23.2.1

# Embeddings / RAG
sentence-transformers>=2.7.0

# Vector database (default)
chromadb>=0.5.3

# File parsing
PyPDF2>=3.0.1

# Utilities
requests>=2.31.0
python-dotenv>=1.0.1  # if you want to load .env automatically

# --- Optional backends (uncomment if you plan to use them) ---
# transformers>=4.41.0    # local text-generation pipeline
# torch>=2.3.0            # required by transformers for local inference
# pinecone-client>=2.2.4  # Pinecone vector DB client
